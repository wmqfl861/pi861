# Pi861 第一版开发计划

状态：开发中。此文件先于实现提交；以 `extensions/pi861/README.md` 和测试报告记录实际交付，不把设计当成已实现。

基线：`c7cdb460aa8a0cebef3446c4166729b8a0d97ead`。
工作分支：`feat/pi861-platform-v1`。不直接提交或合并到 main。

## 1. 产品目标

在 Pi 上开发自己的原生 Agent 平台，不包装 Codex、Claude Code 等外部 Agent 产品。质量优先，在同等验收要求下减少昂贵模型、重复上下文、无效重试和无用调度。简单任务走快速路径，复杂任务采用可调整计划和持续调度。

首版通过独立、可显式加载的 Pi 扩展与可测试核心提供功能，避免修改供应商适配和既有会话格式。默认不自动启动后台任务、安装第三方插件、调用收费模型、连接生产数据库、发布 npm 或部署服务。启用配置与 `/goal` 才授权对应运行；任何实测范围与限制必须公开。

## 2. 已确认需求与不可破坏的边界

### R01 模型能力路由

- 每个 Agent 配置多个模型。一次入口判断同时选择直接完成、固定或动态模式及初始模型，不堆叠多个路由 Agent。
- 固定模式正常不重选，出现任务变化、验证失败、无进展或明确能力障碍时可重新评估。
- 动态模式在阶段/事件边界升降档；能力等级是配置和评测结论，不等于价格排序。
- 子 Agent 根据子任务选择模型，使用同一策略和全局预算。换模型不换身份、不扩大权限。
- 质量约束、输入类型、上下文容量和允许的数据目的地先过滤，再优化费用。

### R02 故障接管与自动回切

- 能力路由与健康路由分开；故障检测与备用选择不再请求一个模型判断。
- 首选配置与当前执行配置分开，记录完整配置标识/版本。
- 自动故障接管与自动回切是两个独立开关，支持继承与 Agent 覆盖。
- 有限重试、总尝试预算、截止时间、退避、服务端等待指示、熔断、有限恢复探测。
- 恢复后在安全执行边界自动回到当前有效首选配置；任务主动改选或配置更新使旧回切失效。
- 用户取消不触发救援。旧尝试的迟到响应不能重新派发工具或覆盖结果。
- 工具结果未知时先核对；无幂等/查询支持的副作用不盲目重试。

### R03 Skill 编译与分层发现

- 原始包按版本完整归档，包含脚本、引用和资源；默认不向模型展示，保留显式调用。
- 全文分析生成能力记录；去重合并公共流程，保留必要的替代分支和兼容补充模块。
- 可观察的适用/排除条件、环境、工具依赖、验收规则与来源是同一个记录的投影。
- 浅目录、关键词搜索、按需加载运行版；向量搜索是可选增强。
- 整合生成候选，验证后发布；冲突不自动覆盖，运行任务绑定确定版本。
- 正文精简不删除权限、禁止事项、前置条件或必要证据；存储元数据不必全部注入。

### R04 岗位与 MCP 懒加载

- 岗位允许范围包含服务、工具操作、账户/资源；模型可见性不是权限。
- 每个对 Agent 开放的 MCP 工具有已发布 Skill 入口覆盖，MCP 与 Skill 允许多对多。
- Skill 激活解析结构化依赖；按当前阶段加载具体工具，不整包加载。
- 浏览不激活，激活不自动产生业务副作用；缺必需能力显式失败。
- 工具接口、内容指纹、参数校验、调用时鉴权独立管理；更新后旧绑定不得静默生效。
- 大结果使用有完整性说明的受控引用和分段读取，不用摘要冒充全量检查。

### R05 持续开发小组与动态计划

- 目标、计划、任务、执行尝试、证据分别建模。计划可滚动细化，但已确认的权限/质量边界不能被模型改写。
- 就绪队列事件驱动派工：完成一个就补位，并重新计算刚解锁的依赖，不等待整批最慢任务。
- 任务有明确交付物、范围、接口版本、依赖和验收条件。区别开发依赖与交付依赖。
- 验证通过不等于集成完成；任务验收由明确证据与门禁决定，不能由一句模型自报完成替代。
- 写入工作区、测试资源隔离；共享资源互斥。Git worktree 不是安全沙箱。
- 项目、节点、模型和全部后代共享预算/容量约束；等待不占可复用执行槽。
- 节点租约、检查点和过期执行权处理；审查积压对新开发派工施加背压。
- 无安全并行空间时允许降并发，并显示等待原因，不为凑人数制造工作。

### R06 多层记忆与 PostgreSQL

- 用途、共享范围、读取深度分开。固定约束/工作记忆/项目事实/长期经验/历史证据各有语义。
- L0 摘要、L1 概览、L2 证据按需读取；关键任务状态直接加载，不依赖模糊召回。
- 自动采集增量事件，受控提炼候选；模型假设不得自动升级为项目事实。
- 来源、有效期、版本、撤回、替代、状态和岗位权限贯穿正文及索引。
- 并发写版本检查、同请求幂等回执、撤回阻止旧任务复活。搜索结果不是新独立证据。
- 外接 PostgreSQL 为多节点主存储；原始大文件可使用独立对象存储；全文与可选 pgvector 为派生索引。
- 数据库密码不进入模型。数据库故障不悄悄切成另一份共享真源；未提交不能报告已保存。
- 自动读/写分别可配置。换模型、换节点、压缩后从最新上下文包恢复。

### R07 搜索

- 复用 Pi 本地 read/grep/find/ls，不重新编写文件搜索引擎。
- 联网搜索作为模型无关的可配置能力，经 Skill 和岗位授权按需加载。
- 支持搜索结果来源、正文按需读取、结果预算和显式降级；禁止把抓取失败称为完整搜索。
- 默认不发送项目私密内容，不因模型切换改变允许的数据目的地。

### R08 Goal

- `/goal <objective>` 建立持久目标；支持 status/edit/pause/resume/clear。
- 目标保存验收条件、预算和任务；持续推进由统一调度器负责，不另开一套自动续跑器。
- 计划生成与结构校验分开，计划变更有版本和影响检查。
- 明确区分 active/paused/blocked/completed/cancelled；资源耗尽不是成功。
- 可选独立审核，程序验收不可被模型放宽。暂停/取消不再派发新工作。

## 3. 架构与交付分段

所有阶段都是本项目范围；下面顺序是依赖关系，不代表每种后台项目固定执行同一路线。

### M0 计划与验证环境

1. 保存此计划并建立独立分支。
2. 核对 Pi 扩展公开接口、仓库规则和既有 CI。
3. 使用无真实凭据的测试；依赖安装禁止生命周期脚本。

### M1 可运行核心与 Goal

- 版本化协议、严格配置校验、原子状态存储、任务状态机、依赖图和持续补位。
- `/goal` 和状态/暂停/恢复入口；确定性假执行器做端到端调度测试。
- 发布 README、示例配置、验收步骤，不把尚未连通真实执行器的逻辑标为生产可用。

### M2 模型路由与可靠性

- 独立路由策略与故障状态机；配置继承、质量过滤、开关组合、恢复探测、安全回切。
- 同请求重试、取消与迟到响应隔离；测试全程使用可控假模型。
- 接入 Pi 原生模型/子会话公开接口，保留模型选择依据与使用量。

### M3 Skill、岗位、工具和搜索

- 原始/候选/已发布运行版状态及来源，目录与关键词索引，分支依赖。
- 程序强制的工具授权、延迟加载及接口版本检查。
- 模型无关联网搜索适配与来源输出；缺配置时明确不可用。

### M4 记忆

- 内存测试后端与持久后端共用契约；先实现来源/版本/范围/撤回，后实现摘要增强。
- 外接 PostgreSQL 适配与迁移；按调用者范围查询及变更游标。
- Pi 自动读写钩子和任务状态衔接，索引不可用时诚实降级。

### M5 分布式与生产加固

- 多节点认证、租约续期、过期执行权拒绝、工作区和测试环境生命周期。
- 后端真实联调、故障注入、压力测试、权限穿透与隐私检查。
- 只有实际完成相应验证，才能标记跨节点生产就绪。单机队列不冒充集群。

## 4. 首版工程约束

- 新实现优先放在 `extensions/pi861/`，以显式 Pi 扩展方式加载；不改变默认 Pi 行为。
- 确定性核心尽量只依赖 Node 标准库，可在无 API Key 环境测试；供应商/数据库通过适配器接入。
- 自动循环、模型调用、联网、数据库访问都需明确配置，不能在安装或导入模块时启动。
- 社区项目用于结构与边界参考。首版不复制 OpenViking AGPL 代码、不自动引入多套运行时；具体依赖先核对固定版本及许可证。
- 不把提示词限制冒充沙箱，不把会话恢复冒充副作用恢复，不把模拟测试冒充真实供应商验证。

## 5. 验收矩阵

- 路由：直接/固定/动态三路径；能力不足升级；固定模式无常态重选；不合格备用被拒绝。
- 可靠性：四种开关组合；断流/限流/超时；取消无接管；多次成功才回切；旧配置探测无效；回切不重做工具。
- 调度：快任务完成即补位；依赖刚解锁即调度；循环依赖拒绝；共享写范围互斥；取消不派新任务；预算耗尽暂停；旧租约不能完成新尝试。
- Skill/MCP：原始内容默认不可见；精确显式入口；未发布候选不可加载；权限失败拒绝；工具指纹变化拒绝；分支只加载需要的接口。
- 记忆：不同项目不可串读；纠正保留来源；旧版本写冲突；同请求重放不重复；撤回后不复活；L0/L1 不假称完整证据；数据库失败不报告提交成功。
- Goal：跨重启恢复；暂停无续跑；没有任务/验收证据不得完成；所有子任务统一计量。
- 搜索：无后端配置明确失败；只对允许的目标发请求；结果截断有提示；测试不访问外网。

## 6. 验证与报告

每次修改测试文件都运行对应测试。按仓库要求尝试 `npm run check` 并记录完整结果；构建环境、依赖或远程服务不可用时如实记录阻断，不能写“全绿”。不运行带真实提供商凭据的端到端测试。

最终报告逐项列出：实际代码路径、运行命令、通过/失败/未执行的测试、已提交 SHA、PR、已知限制及下一阶段。不会自动合并或发布。

## 7. 设计参考（不表示已复制代码或验证全部能力）

- https://github.com/QuintinShaw/pi-dynamic-workflows — 编排、嵌套预算和恢复。
- https://github.com/nicobailon/pi-messenger — 团队、任务、协作与队列。
- https://github.com/tintinweb/pi-subagents — 原生子会话与生命周期。
- https://github.com/nicobailon/pi-subagents — Mission/Run/Receipt 划分。
- https://github.com/nicobailon/pi-mcp-adapter — 元数据缓存与懒连接。
- https://github.com/tmonk/pi-goal-x — 目标控制与完成审核。
- https://github.com/nicobailon/pi-web-access — 搜索与正文按需读取。
- https://github.com/volcengine/OpenViking — 分层目录与 Pi 集成设计，代码复用需许可证审查。
- https://github.com/garrytan/gbrain — PostgreSQL、来源、版本和持久回执。
- https://github.com/chandra447/pi-hermes-memory — 固定约束与自动记忆分离。
- https://github.com/elpapi42/pi-observational-memory — 来源链、工作记忆与压缩恢复。
